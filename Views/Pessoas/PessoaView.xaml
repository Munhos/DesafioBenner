<UserControl x:Class="WpfApp.Views.PessoaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="450">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Border Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- DataGrid editável -->
            <DataGrid Grid.Row="0"
                      ItemsSource="{Binding Pessoas}"
                      SelectedItem="{Binding PessoaSelecionada, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      RowHeight="30"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                    <DataGridTextColumn Header="CPF" Binding="{Binding CPF, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                    <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Botões de ação -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Incluir" Width="80" Command="{Binding AbrirNovaPessoaCommand}" Margin="0,0,5,0"/>
                <Button Content="Editar" Width="80" Command="{Binding EditarPessoaCommand}" 
                        IsEnabled="{Binding PessoaSelecionada, Converter={StaticResource BoolToVis}}" Margin="0,0,5,0"/>
                <Button Content="Excluir" Width="80" Command="{Binding ExcluirPessoaCommand}" 
                        IsEnabled="{Binding PessoaSelecionada, Converter={StaticResource BoolToVis}}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
